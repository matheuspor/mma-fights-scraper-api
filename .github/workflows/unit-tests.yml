name: Unit-tests # name of the action (displayed in the github interface)
 
on: [pull_request] # on a pull request to each of these branches
 
jobs: # list of things to do
  Unit-tests:
    name: Unit-tests # job name (unique id)
    runs-on: ubuntu-latest # on which machine to run
    steps: # list of steps
      - name: Install NodeJS
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Code Checkout
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm ci

      - name: Unit-tests
        run: npm run test